import { DScrollViewDirective } from './d-scroll-view.directive';

describe('DScrollViewDirective', () => {
  it('should create an instance', () => {
    const directive = new DScrollViewDirective();
    expect(directive).toBeTruthy();
  });
});
